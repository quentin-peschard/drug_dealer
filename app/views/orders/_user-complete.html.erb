<div class="flash flash-warning alert alert-dismissible fade show" role="alert">
  <div class="flash-content">
    <p><strong>Hi again!</strong> Your order is being delivered as we speak! It will arrive later today ☺️</p>
    <%= link_to "< Back to your orders", orders_path, class:"button-flash-index"%>
  </div>
  <a data-dismiss="alert" aria-label="Close">
    <i class="fas fa-times"></i>
  </a>
</div>

<div class="pending-view">
  <div class="cart-header">
    <h2>My cart</h2>
  </div>
  <div class="main">
    <% if @order.prescriptions.attached? %>
      <% @order.prescriptions.each do |prescription| %>
        <div class=main2>
          <%= cl_image_tag prescription.key, class: 'card-prescription' %>
        </div>
      <% end %>
    <% end %>
  </div>
  <% @ordered_drugs.each do |ordered_drug| %>
    <div class="cart-card">
      <% if ordered_drug.drug.pic.attached?%>
        <%=cl_image_tag ordered_drug.drug.pic.key%>
      <%else%>
        <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
      <%end%>
      <div class="cart-card-infos">
        <h2><%=ordered_drug.drug.name.downcase.capitalize%></h2>
        <div class=cart-card-bottom>
          <h6><small><%= ordered_drug.quantity %> x </small> <%= ordered_drug.price %> $</h6>
        </div>
      </div>
    </div>
  <% end %>
  <div class="total-price">
    <h3>Total</h3>
    <h2>$<%= @order.total_price %></h2>
  </div>
</div>
