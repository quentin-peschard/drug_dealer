<% if !user.admin? && @order.status == 'initialize' %>
  <%= render 'orders/user-initialize' %>
<% elsif !user.admin? && @order.status == 'pending' %>
  <%= render 'orders/user-pending' %>
<% elsif !user.admin? && @order.status == 'ready' %>
  <%= render 'orders/user-ready' %>
<% elsif !user.admin? && @order.status == 'complete' %>
  <%= render 'orders/user-complete' %>
<% elsif user.admin? && @order.status == 'pending' %>
  <%= render 'orders/pharm-pending' %>
<% end %>
<br><br><br><br><br><br><br><br>
